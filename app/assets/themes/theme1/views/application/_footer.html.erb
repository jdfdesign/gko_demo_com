<%# cache("#{site.id.to_s}_#{I18n.locale.to_s}_site_footer") do %>
    <!-- Footer
   ================================================== -->
    <footer id='footer-container' class='container-fluid hide-on-print'>
      <div class='row-fluid' id="main-footer-row">
        <div class="span4">
            <h3><%= "Newsletter".html_safe %></h3>
            <p><%= t(:"newsletters.register_you") %></p>
            <%= render("newsletter_subscriptions/form") %>
        </div>
        <div class="span4">
            <h3><%= "Get in touch with the studio".html_safe %></h3>
            <p><%= "<strong>T.</strong> +590. 590 29 20 19".html_safe %></p>
            <p><%= "<strong>C.</strong> +590. 690 58 52 74".html_safe %></p>
            <p><%= "<strong>E.</strong> contact@joufdesign.com".html_safe %></p>
        </div>
        <div class="span4">
    
        </div>
      </div>
      <div class='row-fluid' id="credit-footer-row">
        <div class="span12" >
          <div class="pull-left">
          <p><small><%= "Copyright © Jouf Design Studio, Web Design Saint-Barthélemy".html_safe %></small></p>
          </div>
          <div class="pull-right">
			<%= render_menu('pipe') %>
            <%= render_menu('tools') %>
			<%= render_menu('legal') %>
          </div>
        </div>
      </div>
    </footer>
<%# end %>